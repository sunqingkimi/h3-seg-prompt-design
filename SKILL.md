---
name: h3-seg-prompt-design
description: 为“噜噜和噜妹”及相近Q版3D动画短视频提供从参考图与剧情概要到剧情节拍、分镜、台词、5–15秒分段H3提示词和Director配置校验的完整工作流。保留素材锁定、连续性、对白、分段与导出校验能力；当用户提供角色参考图、剧情概要、总时长并希望生成多段H3视频提示词时使用。
metadata:
  short-description: Lولو/噜妹 3D cartoon short-video director and H3 segmented prompt builder
  version: 2.1.0-cartoon-specialized
---

# 噜噜和噜妹 3D 动画短视频导演 Skill

## 目标

将用户提供的**参考图 + 剧情概要 + 总时长 + 风格偏好**，转化为可审阅、可校验、可直接交给 H3 分段生成的完整制作包：

1. 项目输入与默认参数
2. 角色圣经与素材映射
3. 剧情节拍与台词草案
4. 自然时长估算后的分镜设计
5. 5–15 秒分段 H3 提示词
6. Director JSON 导出与校验报告

本 Skill 默认服务于：Q版、3D、可爱、情侣日常、轻喜剧、治愈、反转类短视频。不得因为“卡通”而牺牲动作物理、角色身份、道具状态、台词归属和镜头连续性。

## 必须读取

开始前完整阅读：

- `references/workflow-rules.md`
- `references/h3-base-guide.md`
- 涉及参考图/视频/音频时阅读 `references/h3-full-reference-guide.md`
- 导出 Director 配置时阅读 `references/theodore-director-plan-v5.md`
- 按需使用 `assets/噜噜噜妹项目输入模板.md`、`assets/噜噜噜妹角色圣经模板.md`、`assets/分镜设计模板.md`、`assets/H3分镜提示词模板.md`

## 交互式输入协议

首次接收任务时，先从用户输入中提取以下字段；缺失项使用默认值并明确标注，不要一次性追问所有次要信息。

### 必填或优先确认

- 参考图：用户上传的角色图、场景图、道具图或首帧图
- 剧情概要：一句话或几句话即可
- 总时长：例如 15/30/60 秒；若未提供，先询问
- 是否需要台词：需要台词/少量台词/无台词

### 默认值

- 画幅：9:16
- 风格：搞笑快节奏；用户未指定时允许根据剧情建议改为温馨治愈、轻松日常或情绪反转
- 角色：默认以参考图为唯一外观依据，不自行发明固定外观
- H3生成段：每段5–15秒，先按内容估时再拆分
- 台词：中文；一镜原则上不超过一句主要台词，除非剧情确实需要
- 画面文字：默认无；不得自行添加字幕、标题、水印或可读招牌

### 缺失信息的处理顺序

1. 影响角色身份、参考图归属、总时长、是否有台词的问题必须先确认。
2. 不影响核心创作的次要项可以给出“暂定方案”，并在输出中列出。
3. 不要因为缺少地点、季节或配乐而停止；可给出少量候选并采用一个明确的暂定值。

## 主工作流

### 阶段A：项目简报

创建或更新项目文件夹，先生成 `项目简报.md`，包含：

- 项目名与一句话卖点
- 总时长目标与容差
- 画幅、风格、节奏模式
- 台词/旁白/音效/配乐需求
- 参考图清单及用户确认状态
- 待确认问题与暂定假设

### 阶段B：角色与素材锁定

创建 `素材映射.md`。所有用户提供的参考素材必须拥有唯一别名、类型、用途、路径（可空）、已确认事实和连续性要求。

对于噜噜与噜妹，必须建立角色圣经，至少包含：

- 物种/模型类型与不可改变的外观特征
- 体型、比例、脸部特征、眼睛、耳朵、毛发/材质
- 固定服装、配饰和颜色
- 常态表情与动作习惯
- 角色关系和性格差异
- 台词DNA：说话人ID、句式标签、语气尾字、口头禅、禁用表达
- 不允许出现的变体：人类化、换种属、额外角色、服装漂移等

素材确认后即锁定：禁止自动添加上一段成片、尾帧、伴音或推断出的新参考素材。

### 阶段C：剧情节拍与台词草案

先生成 `剧情节拍.md` 或在分镜文件中建立剧情节拍区。每个节拍写清：

- 触发事件
- 角色目标
- 可见行动
- 情绪变化
- 台词或无台词表达
- 节拍结束状态

为每句台词执行：角色归属、动作互赖、口语自然度、字数与时长、问答闭环、与画面因果对齐检查。不要强迫每个段落都反转；根据节奏模式选择“搞笑反转、萌甜互动、温柔收束、悬念接力”之一。

### 阶段D：分镜设计

必须先设计镜头内容，再估算自然时长，最后确定分段数量。禁止总时长除以固定段长后填内容。

每段必须：

- 5–15秒（含5和15）
- 有明确起始状态、动作过程和结束状态
- 描述景别、构图、机位、运镜、角色位置、视线、表情、道具状态、声音和台词
- 写明场景锚、布局复述、光色、道具手位
- 标记开场钩子、情绪线、结尾钩子，但允许治愈/日常段使用柔和钩子
- 记录时长依据
- 记录实际使用的已锁定素材

若连续未剪切长镜头超过15秒，只能在自然动作节点拆分；从第二段起标记 `（接续上一个分镜）` 和 `长镜头跨段：是（接续上一个分镜）`。分镜提示词代码块仍必须独立，不得要求H3读取其他生成段。

### 阶段E：用户确认门

在写最终H3提示词前，先输出“分镜确认包”，让用户确认：

1. 剧情方向和结尾
2. 角色行为是否符合人设
3. 台词是否保留或修改
4. 总时长和分段数量
5. 参考素材映射是否正确

用户未确认时，只修改剧情、分镜和台词，不生成最终提示词代码块。

### 阶段F：H3提示词

确认后生成 `H3分镜提示词.md`。每个代码块必须是可独立提交的请求：

- 正文结构遵循H3官方指南
- 字段正文使用英文；对白和可见画面文字保留在 `<d>` 中的原文
- 只使用素材映射中已经确认的 `{{ref:别名}}`
- 保留稳定的 `<Subject N>` 和说话人ID
- 当前段内完整写出角色外观、当前姿态、环境、动作、声音和结束状态
- 不在代码块中出现“上一段、下一段、延续、接力、跨段”等跨段指令
- 无对白时不要出现 `<d>`；无画面文字时不要主动设计文字

### 阶段G：导出与校验

执行：

```powershell
python scripts/export_director_plan.py "<视频项目文件夹>"
python scripts/validate_project.py "<视频项目文件夹>"
python scripts/check_anchors.py "<视频项目文件夹>"
```

先修复错误，再报告警告。`check_anchors.py` 审计人物/道具锚点是否在提示词阶段写全（座位绑定、区分物、道具总数锁、否定锁、逐镜Subject引用；未定义的Subject直接报错）。空素材路径必须明确告知用户：Director配置可导入，但不能直接执行，需在导播台补充路径或上传素材。

## 输出顺序

默认按以下顺序输出，避免一次性把用户淹没在最终提示词中：

1. 项目简报
2. 角色/素材映射与缺失信息
3. 剧情节拍与台词草案
4. 分镜设计与时长汇总
5. 等待用户确认
6. H3分段提示词
7. 校验结果与使用说明

用户明确要求“一次性输出”时，仍按上述章节组织，但可在同一回复中全部给出。

## 不可违背的规则

- 每段5–15秒；总时长默认允许约±10%弹性，用户明确要求严格时长时以用户要求为准。
- 单段最多9张参考图、3个参考视频、3条参考音频。
- 不新增用户未确认的素材。
- 角色、Subject编号、Speaker ID、服装、光线、空间和道具状态必须稳定。
- 台词与分镜中的对白逐字一致。
- 角色说话时才张嘴；静默角色保持闭嘴或只做反应。
- 不生成字幕、水印、随机Logo或未经要求的可读文字。
- 不能用“很可爱、很生动、很有电影感”替代具体可见动作。
- 脚本通过不等于创意通过；仍需人工阅读相邻镜头和实际参考图。

## 资源

- `assets/噜噜噜妹项目输入模板.md`
- `assets/噜噜噜妹角色圣经模板.md`
- `assets/分镜设计模板.md`
- `assets/H3分镜提示词模板.md`
- `scripts/export_director_plan.py`
- `scripts/validate_project.py`
- `scripts/check_anchors.py`
